package hashFunction;

/**
 * The enumeration of method types that can be used by hash functions of closed
 * address hash tables.
 * 
 */
public enum ClosedAddressMethod {
	DIVISION,
	MULTIPLICATION;
}
